- name: Install k3s
  shell: |
    curl -sfL https://get.k3s.io | sh -
  args:
    creates: /usr/local/bin/k3s

- name: Wait for node
  shell: kubectl get nodes
  register: result
  retries: 10
  delay: 5
  until: result.rc == 0