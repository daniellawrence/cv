{{- if .Values.misql.enabled }}
{{- $envValue := .Values.env.DATABASE_URL -}}
apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
type: Opaque
data:
  url: {{ default "" $envValue | b64enc }}
{{- end }}