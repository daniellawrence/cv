service:
  type: ClusterIP

# Dev environment uses local registry with service-specific names
# Naming pattern: registry:5000/<service>:latest
image:
  repo: registry:5000
  tag: latest

ingress:
  enabled: true
  className: traefik
  domain: localhost
  schema: http
