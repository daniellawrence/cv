replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8080

ingress:
  enabled: true
  className: traefik
  host: basic-app.local
  path: /
  domain: localhost

frontendruntimeconfig:
  enabled: false