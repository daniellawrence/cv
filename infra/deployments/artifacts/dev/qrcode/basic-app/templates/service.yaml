---
# Source: basic-app/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: qrcode
  labels:
    app: qrcode
    deploy-timestamp: "1773208329"
spec:
  type: ClusterIP
  selector:
    app: qrcode
  ports:
    - port: 8080
      targetPort: 
      protocol: TCP
