---
# Source: basic-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: experience
  labels:
    app: experience
    deploy-timestamp: "1773208329"
spec:
  ingressClassName: traefik
  rules:
    - host: experience.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: experience
                port:
                  number: 8080
