---
# Source: basic-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: interest
  labels:
    app: interest
    deploy-timestamp: "1773208329"
spec:
  ingressClassName: traefik
  rules:
    - host: interest.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: interest
                port:
                  number: 8080
