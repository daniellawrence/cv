---
# Source: basic-app/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: interest
spec:
  type: ClusterIP
  selector:
    app: interest
  ports:
    - port: 8080
      targetPort: 
      protocol: TCP
