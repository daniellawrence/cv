---
# Source: basic-app/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: identity
spec:
  type: ClusterIP
  selector:
    app: identity
  ports:
    - port: 8080
      targetPort: 
      protocol: TCP
