---
# Source: basic-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: identity
  labels:
    app: identity
    deploy-timestamp: "1773208556"
spec:
  ingressClassName: traefik
  rules:
    - host: identity.dansysadm.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: identity
                port:
                  number: 8080
