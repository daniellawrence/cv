---
# Source: basic-app/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qrcode
  labels:
    app: qrcode
    deploy-timestamp: "1773208556"
spec:
  ingressClassName: traefik
  rules:
    - host: qrcode.dansysadm.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qrcode
                port:
                  number: 8080
