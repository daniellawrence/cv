apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cv-experience-dev
  namespace: argocd
spec:
  project: cv-dev
  source:
    repoURL: https://github.com/daniellawrence/cv
    targetRevision: HEAD
    path: infra/deployments/artifacts/dev/experience/basic-app/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: experience
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
