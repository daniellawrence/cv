apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cv-qrcode-dev
  namespace: argocd
spec:
  project: cv-dev
  source:
    repoURL: https://github.com/daniellawrence/cv
    targetRevision: HEAD
    path: infra/deployments/artifacts/dev/qrcode/basic-app/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: qrcode
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
