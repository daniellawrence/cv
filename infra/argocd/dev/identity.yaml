apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cv-identity-dev
  namespace: argocd
spec:
  project: cv-dev
  source:
    repoURL: https://github.com/daniellawrence/cv
    targetRevision: HEAD
    path: infra/deployments/artifacts/dev/identity/basic-app/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: identity
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
